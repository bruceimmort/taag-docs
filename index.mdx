---

title: 'Introduction'
description: "Our pricing engine handles and let's you implement dynamic, location-based pricing so you can offer affordable pricing to everyone and everywhere."
---

## Base URL

Our pricing API is built on REST principles keeping things smooth and reliable. HTTPS is mandatory on every request. (No HTTP allowed here!)

All your requests start with this base URL:

```bash
https://taag-api.carllabs.cc
```

## Authentication

To get going, just add an Authorization header to your request. Always include your **secret key** in all server-side calls and **public key** in client-side (like browser) calls. 


**Public key** looks like this
```bash
Authorization: taag_pk_xxxxxxxxx
```

**Secret key** looks like this
```bash
Authorization: taag_sk_xxxxxxxxx
```

<Warning>
  Keep your **secret key** safe and don’t share it around!
</Warning>

## API Endpoints

So why not integrate now:

<CardGroup cols={2}>
  <Card
    title="Get Price"
    icon="tag"
    href="./api-reference/v1/pricing/get"
  >
    Get the right price for your users based on their region.
  </Card>
  <Card
    title="Verify Price"
    icon="shield-check"
    href="./api-reference/v1/pricing/verify"
  >
    Check if a price is correct before checking out.
  </Card>
  <Card
    title="Add Subscriber"
    icon="user-plus"
    href="./api-reference/v1/subcribers/new"
  >
    Add a new subscriber in just one step.
  </Card>
  <Card
    title="Verify Subscriber"
    icon="user-check"
    href="./api-reference/v1/subcribers/verify"
  >
    Confirm if a subscriber exists or is active.
  </Card>
</CardGroup>

Set prices smartly while building products that scale.

<Card
  title="Pricing Endpoints"
  href="./api-reference/v1/pricing"
>
  Adapt pricing the way top market leaders already do.
</Card>